# Contact form email delivery (Resend — https://resend.com)
# Create an API key in the Resend dashboard and add these in Vercel:
#   Project → Settings → Environment Variables
RESEND_API_KEY=

# Optional overrides (sensible defaults are baked in):
# CONTACT_TO=hello@journeytravel.group
# CONTACT_FROM=Journey Travel Group <hello@journeytravel.group>
